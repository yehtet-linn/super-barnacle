let myName: string = "Ye";
let meaningOfLife: number;
let isLoading: boolean = false;
let album: any;

myName = "Johnson";
meaningOfLife = 5;
isLoading = true;
album = 455;

const sum = (a: number, b: string) => {
  return a + b;
};

let postId: string | number;
let isActive: number | boolean;

let exp: RegExp = /\w+/g;
